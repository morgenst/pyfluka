language: python
python:
  - "2.7"
# command to install dependencies
install: python setup.py install
# command to run tests
script:
  nosetests tests --with-coverage --cover-package=pyfluka
after_success:
  - coveralls